// visio/control/v1/command.proto
//
// Host-to-device control surface on the CONTROL_STREAM_COMMAND control
// stream. Visio addition; UMI v3 did not have a first-class command type
// (control flow lived in config files and out-of-band sockets).
//
// Target is addressed by device name (the DeviceInfo.device_name a device
// announces). Empty target = broadcast to every device.

// @generated by protoc-gen-es v2.12.0 with parameter "target=ts,import_extension=js"
// @generated from file visio_schema/v1/control/command.proto (package visio_schema.v1.control, syntax proto3)
/* eslint-disable */

import type { GenEnum, GenFile, GenMessage } from "@bufbuild/protobuf/codegenv2";
import { enumDesc, fileDesc, messageDesc } from "@bufbuild/protobuf/codegenv2";
import type { CameraCalibration } from "../../../foxglove/CameraCalibration_pb.js";
import { file_foxglove_CameraCalibration } from "../../../foxglove/CameraCalibration_pb.js";
import type { FrameTransform } from "../../../foxglove/FrameTransform_pb.js";
import { file_foxglove_FrameTransform } from "../../../foxglove/FrameTransform_pb.js";
import type { ImuCalibration } from "../calibration/imu_pb.js";
import { file_visio_schema_v1_calibration_imu } from "../calibration/imu_pb.js";
import type { EncoderCalibration } from "../calibration/encoder_pb.js";
import { file_visio_schema_v1_calibration_encoder } from "../calibration/encoder_pb.js";
import type { CameraTuning } from "../calibration/camera_tuning_pb.js";
import { file_visio_schema_v1_calibration_camera_tuning } from "../calibration/camera_tuning_pb.js";
import type { Message } from "@bufbuild/protobuf";

/**
 * Describes the file visio_schema/v1/control/command.proto.
 */
export const file_visio_schema_v1_control_command: GenFile = /*@__PURE__*/
  fileDesc("", [file_foxglove_CameraCalibration, file_foxglove_FrameTransform, file_visio_schema_v1_calibration_imu, file_visio_schema_v1_calibration_encoder, file_visio_schema_v1_calibration_camera_tuning]);

/**
 * @generated from message visio_schema.v1.control.Command
 */
export type Command = Message<"visio_schema.v1.control.Command"> & {
  /**
   * Intended recipient, by device name. A receiving peer MUST ignore
   * Commands whose target_device is neither empty (broadcast) nor its own
   * device name.
   *
   * @generated from field: string target_device = 1;
   */
  targetDevice: string;

  /**
   * Per-sender monotonic counter; useful for matching to acks if a
   * future ack mechanism is added.
   *
   * @generated from field: uint64 command_id = 2;
   */
  commandId: bigint;

  /**
   * @generated from oneof visio_schema.v1.control.Command.body
   */
  body: {
    /**
     * @generated from field: visio_schema.v1.control.StartRecording start_recording = 10;
     */
    value: StartRecording;
    case: "startRecording";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.StopRecording stop_recording = 11;
     */
    value: StopRecording;
    case: "stopRecording";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.Identify identify = 12;
     */
    value: Identify;
    case: "identify";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetAutoStart set_auto_start = 13;
     */
    value: SetAutoStart;
    case: "setAutoStart";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.ConnectWifi connect_wifi = 14;
     */
    value: ConnectWifi;
    case: "connectWifi";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.ScanWifi scan_wifi = 15;
     */
    value: ScanWifi;
    case: "scanWifi";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetStorage set_storage = 16;
     */
    value: SetStorage;
    case: "setStorage";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.TestStorage test_storage = 17;
     */
    value: TestStorage;
    case: "testStorage";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.ListRecordings list_recordings = 18;
     */
    value: ListRecordings;
    case: "listRecordings";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.GetState get_state = 19;
     */
    value: GetState;
    case: "getState";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetCalibration set_calibration = 20;
     */
    value: SetCalibration;
    case: "setCalibration";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetTime set_time = 21;
     */
    value: SetTime;
    case: "setTime";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetRecordingMeta set_recording_meta = 22;
     */
    value: SetRecordingMeta;
    case: "setRecordingMeta";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetVideoStreaming set_video_streaming = 23 [deprecated = true];
     * @deprecated
     */
    value: SetVideoStreaming;
    case: "setVideoStreaming";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetBitrate set_bitrate = 24;
     */
    value: SetBitrate;
    case: "setBitrate";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.FormatStorage format_storage = 25;
     */
    value: FormatStorage;
    case: "formatStorage";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetAutoUpload set_auto_upload = 26;
     */
    value: SetAutoUpload;
    case: "setAutoUpload";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetNoticeLang set_notice_lang = 27;
     */
    value: SetNoticeLang;
    case: "setNoticeLang";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetResolution set_resolution = 28;
     */
    value: SetResolution;
    case: "setResolution";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.ResetToAp reset_to_ap = 29;
     */
    value: ResetToAp;
    case: "resetToAp";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetAudioRecording set_audio_recording = 30;
     */
    value: SetAudioRecording;
    case: "setAudioRecording";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetImuLiveRate set_imu_live_rate = 31 [deprecated = true];
     * @deprecated
     */
    value: SetImuLiveRate;
    case: "setImuLiveRate";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetStreamPolicy set_stream_policy = 32;
     */
    value: SetStreamPolicy;
    case: "setStreamPolicy";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.ForgetWifi forget_wifi = 33;
     */
    value: ForgetWifi;
    case: "forgetWifi";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetStatusReport set_status_report = 34;
     */
    value: SetStatusReport;
    case: "setStatusReport";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.ClearCameraTuning clear_camera_tuning = 35;
     */
    value: ClearCameraTuning;
    case: "clearCameraTuning";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetRecordingDestination set_recording_destination = 36;
     */
    value: SetRecordingDestination;
    case: "setRecordingDestination";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetRecordingHeartbeat set_recording_heartbeat = 37;
     */
    value: SetRecordingHeartbeat;
    case: "setRecordingHeartbeat";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetNoticeVolume set_notice_volume = 38;
     */
    value: SetNoticeVolume;
    case: "setNoticeVolume";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetRecordingKey set_recording_key = 39;
     */
    value: SetRecordingKey;
    case: "setRecordingKey";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetHandDetection set_hand_detection = 40;
     */
    value: SetHandDetection;
    case: "setHandDetection";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetGpsTagging set_gps_tagging = 41;
     */
    value: SetGpsTagging;
    case: "setGpsTagging";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.SetDiagVerbosity set_diag_verbosity = 42;
     */
    value: SetDiagVerbosity;
    case: "setDiagVerbosity";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.OpenRecordingFile open_recording_file = 43;
     */
    value: OpenRecordingFile;
    case: "openRecordingFile";
  } | {
    /**
     * @generated from field: visio_schema.v1.control.DeleteRecording delete_recording = 44;
     */
    value: DeleteRecording;
    case: "deleteRecording";
  } | { case: undefined; value?: undefined };
};

/**
 * Describes the message visio_schema.v1.control.Command.
 * Use `create(CommandSchema)` to create a new message.
 */
export const CommandSchema: GenMessage<Command> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 0);

/**
 * Enable/disable the periodic tick-tock voice heartbeat a speaker-equipped
 * board plays while it is recording (the recording_tick_tock notice, every
 * 5 s). Persisted to a device-side sidecar and applied WITHOUT a reboot — an
 * in-flight recording goes quiet (or starts ticking) on the next beat. Only the
 * heartbeat is affected: recording start/stop announcements and error notices
 * keep playing.
 *
 * Only boards with a speaker + voice notices accept this; every other device
 * rejects it ("unsupported"), which is EXPECTED. The boot default comes from
 * the board config (shipping boards default OFF, so the wearer opts in from
 * the app); once the app has set a value, the persisted sidecar wins over the
 * config default on every later boot. The current value is reported in
 * DeviceState.recording_heartbeat, which also tells the app whether to offer
 * the control at all.
 *
 * Note on downgrade: the sidecar survives a rollback to firmware that
 * predates this command, and such firmware plays the heartbeat
 * unconditionally — so a device that was silenced resumes ticking until
 * re-upgraded.
 *
 * @generated from message visio_schema.v1.control.SetRecordingHeartbeat
 */
export type SetRecordingHeartbeat = Message<"visio_schema.v1.control.SetRecordingHeartbeat"> & {
  /**
   * @generated from field: bool enabled = 1;
   */
  enabled: boolean;
};

/**
 * Describes the message visio_schema.v1.control.SetRecordingHeartbeat.
 * Use `create(SetRecordingHeartbeatSchema)` to create a new message.
 */
export const SetRecordingHeartbeatSchema: GenMessage<SetRecordingHeartbeat> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 1);

/**
 * Select where recording controls write. PHONE is deliberately a short lease:
 * the app refreshes it while a lossless USB connection is alive; expiry, app
 * crash, or disconnect safely restores DEVICE/SD behavior.
 *
 * @generated from message visio_schema.v1.control.SetRecordingDestination
 */
export type SetRecordingDestination = Message<"visio_schema.v1.control.SetRecordingDestination"> & {
  /**
   * @generated from field: visio_schema.v1.control.SetRecordingDestination.Destination destination = 1;
   */
  destination: SetRecordingDestination_Destination;

  /**
   * PHONE only; device clamps to a safe short maximum
   *
   * @generated from field: uint32 lease_ms = 2;
   */
  leaseMs: number;
};

/**
 * Describes the message visio_schema.v1.control.SetRecordingDestination.
 * Use `create(SetRecordingDestinationSchema)` to create a new message.
 */
export const SetRecordingDestinationSchema: GenMessage<SetRecordingDestination> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 2);

/**
 * @generated from enum visio_schema.v1.control.SetRecordingDestination.Destination
 */
export enum SetRecordingDestination_Destination {
  /**
   * @generated from enum value: DESTINATION_DEVICE = 0;
   */
  DEVICE = 0,

  /**
   * @generated from enum value: DESTINATION_PHONE = 1;
   */
  PHONE = 1,
}

/**
 * Describes the enum visio_schema.v1.control.SetRecordingDestination.Destination.
 */
export const SetRecordingDestination_DestinationSchema: GenEnum<SetRecordingDestination_Destination> = /*@__PURE__*/
  enumDesc(file_visio_schema_v1_control_command, 2, 0);

/**
 * Begin a recording session on the target.
 *
 * @generated from message visio_schema.v1.control.StartRecording
 */
export type StartRecording = Message<"visio_schema.v1.control.StartRecording"> & {
  /**
   * Optional session name. If empty, the target picks one.
   *
   * @generated from field: string session_name = 1;
   */
  sessionName: string;

  /**
   * ── capture metadata (host-supplied; persisted into the session's
   * session.json sidecar). All optional; empty/zero means "unknown".
   *
   * free-text capture task / label
   *
   * @generated from field: string task = 2;
   */
  task: string;

  /**
   * human-readable place (user-entered)
   *
   * @generated from field: string location = 3;
   */
  location: string;

  /**
   * GPS; 0 when unknown
   *
   * @generated from field: double latitude = 4;
   */
  latitude: number;

  /**
   * GPS; 0 when unknown
   *
   * @generated from field: double longitude = 5;
   */
  longitude: number;

  /**
   * host capture timestamp (us since Unix epoch, UTC)
   *
   * @generated from field: int64 client_unix_us = 6;
   */
  clientUnixUs: bigint;

  /**
   * minutes east of UTC (+480 = UTC+8); for
   *
   * @generated from field: sint32 client_utc_offset_min = 7;
   */
  clientUtcOffsetMin: number;

  /**
   * on-device local time = utc + offset
   *
   * capture frame rate; 0 = use the device's config
   *
   * @generated from field: uint32 fps = 8;
   */
  fps: number;
};

/**
 * Describes the message visio_schema.v1.control.StartRecording.
 * Use `create(StartRecordingSchema)` to create a new message.
 */
export const StartRecordingSchema: GenMessage<StartRecording> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 3);

/**
 * End the current recording session on the target.
 *
 * @generated from message visio_schema.v1.control.StopRecording
 */
export type StopRecording = Message<"visio_schema.v1.control.StopRecording"> & {
};

/**
 * Describes the message visio_schema.v1.control.StopRecording.
 * Use `create(StopRecordingSchema)` to create a new message.
 */
export const StopRecordingSchema: GenMessage<StopRecording> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 4);

/**
 * Ask the target to publish its DeviceInfo immediately, out of the
 * normal cadence. Replaces UMI v3's T_DEVICE_INFO_REQ query.
 *
 * @generated from message visio_schema.v1.control.Identify
 */
export type Identify = Message<"visio_schema.v1.control.Identify"> & {
};

/**
 * Describes the message visio_schema.v1.control.Identify.
 * Use `create(IdentifySchema)` to create a new message.
 */
export const IdentifySchema: GenMessage<Identify> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 5);

/**
 * Select which streams this connection wants, and how fast — the one
 * per-connection delivery filter. Scoped to the connection the Command arrives
 * on, NOT the device: recording to disk and every other connected client are
 * unaffected, which is what lets a preview thin its own feed without touching
 * what gets captured.
 *
 * `target_device` is IGNORED. A policy describes what one link should carry, so
 * it applies at the hop it arrives on. On a suit that is the hub->phone Wi-Fi
 * leg, which is the bandwidth that actually binds.
 *
 * Each SetStreamPolicy REPLACES the connection's policy outright — rules are
 * not merged with what came before. A client therefore sends its full desired
 * state every time, and a lost or reordered command can't leave a half-applied
 * filter behind. A fresh connection has NO policy and so gets everything at
 * full rate: a dropped command costs bandwidth, never data, and a client that
 * records from the live stream stays lossless by never asking.
 *
 * Rules are ORDERED and FIRST MATCH WINS, with an implicit "keep everything
 * else at full rate" tail. So a preview asks for one eye like this:
 *
 *     **\/camera/0     keep          (max_rate_hz 0, drop false)
 *     **\/camera/*     drop          the other eye
 *     **\/imu/*\/quat   drop          the ~470/s fused stream nothing renders
 *
 * Topic patterns are globs over path segments: the leading '/' is stripped from
 * both sides, then '*' matches exactly one segment and '**' matches zero or more
 * segments at any position. Anything else is a literal segment; there are no
 * partial-segment globs ("cam*" matches nothing). Patterns are re-resolved
 * whenever the peer learns a new channel, so a rule covers leaves that a hub has
 * not discovered yet.
 *
 * Prefer a LEADING '**' when the depth is not yours to know. Relayed leaf topics
 * normally arrive unchanged (they already carry the leaf's device_name, e.g.
 * "/head/camera/0"), but a relay MAY namespace them under the leaf's name
 * ("/head/camera/0" -> "/GILABS-AABBCCDD/head/camera/0") — an opt-in used for
 * multi-device bring-up, where two same-role units would otherwise collide on
 * every topic. "**\/camera/0" matches both forms; "*\/camera/0" only the first.
 *
 * Answered by a CommandResult (ok), correlated by Command.command_id.
 *
 * @generated from message visio_schema.v1.control.SetStreamPolicy
 */
export type SetStreamPolicy = Message<"visio_schema.v1.control.SetStreamPolicy"> & {
  /**
   * At most 12 rules — the bound the device's static decode buffer is sized
   * for. A longer list fails to decode as a whole, and carries no command_id to
   * report against, so senders must stay inside it.
   *
   * @generated from field: repeated visio_schema.v1.control.SetStreamPolicy.Rule rules = 1;
   */
  rules: SetStreamPolicy_Rule[];
};

/**
 * Describes the message visio_schema.v1.control.SetStreamPolicy.
 * Use `create(SetStreamPolicySchema)` to create a new message.
 */
export const SetStreamPolicySchema: GenMessage<SetStreamPolicy> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 6);

/**
 * @generated from message visio_schema.v1.control.SetStreamPolicy.Rule
 */
export type SetStreamPolicy_Rule = Message<"visio_schema.v1.control.SetStreamPolicy.Rule"> & {
  /**
   * Glob over the fully-qualified topic (see the grammar above).
   *
   * @generated from field: string topic = 1;
   */
  topic: string;

  /**
   * Send nothing at all on matching streams. Takes precedence over
   * max_rate_hz — a rule is a drop or a cap, never both.
   *
   * @generated from field: bool drop = 2;
   */
  drop: boolean;

  /**
   * Cap matching streams to this many messages per second, per stream.
   * 0 = full rate.
   *
   * IGNORED for camera video. H.265 is inter-coded: shedding arbitrary
   * P-frames costs the decoder its reference chain and blanks the viewer for
   * a whole GOP, so a video stream is keep-or-drop only. Meaningful for
   * self-contained per-sample streams (the fused IMU quaternions) and for
   * bundles (raw IMU), where each message stands alone.
   *
   * @generated from field: uint32 max_rate_hz = 3;
   */
  maxRateHz: number;
};

/**
 * Describes the message visio_schema.v1.control.SetStreamPolicy.Rule.
 * Use `create(SetStreamPolicy_RuleSchema)` to create a new message.
 */
export const SetStreamPolicy_RuleSchema: GenMessage<SetStreamPolicy_Rule> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 6, 0);

/**
 * DEPRECATED — use SetStreamPolicy, which expresses this as
 * `{topic: "*\/camera/**", drop: true}` and can also spare a single camera.
 *
 * Pause/resume camera video streaming to THIS bus client only (addressed by the
 * connection the Command arrives on — not a device-wide switch). Recording to
 * disk and any other connected client are unaffected. The app pauses streaming
 * on screens that don't render video (e.g. the recordings list) so a control
 * reply (ListRecordings, etc.) isn't stuck behind buffered H.265 on a slow
 * Wi-Fi AP. A fresh connection always starts streaming (paused state is not
 * persisted), so a reconnect auto-resumes.
 *
 * Still honoured for fielded app builds: a receiver expands it into the
 * equivalent policy (drop every foxglove.CompressedVideo channel), so old and
 * new clients share one enforcement path.
 *
 * @generated from message visio_schema.v1.control.SetVideoStreaming
 * @deprecated
 */
export type SetVideoStreaming = Message<"visio_schema.v1.control.SetVideoStreaming"> & {
  /**
   * true = stream video to me; false = pause it
   *
   * @generated from field: bool enabled = 1;
   */
  enabled: boolean;
};

/**
 * Describes the message visio_schema.v1.control.SetVideoStreaming.
 * Use `create(SetVideoStreamingSchema)` to create a new message.
 * @deprecated
 */
export const SetVideoStreamingSchema: GenMessage<SetVideoStreaming> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 7);

/**
 * DEPRECATED — use SetStreamPolicy, which expresses this as
 * `{topic: "*\/imu/*\/quat", max_rate_hz: N}` and can also shed the raw bundles.
 *
 * Cap the live delivery rate of per-sample derived IMU streams (the fused
 * quaternions) to THE SENDING CONNECTION only — same per-endpoint scoping as
 * SetVideoStreaming. 0 (and a fresh connection) = full rate, so a client that
 * records from the live stream and never asks stays lossless; a preview-only
 * client requests e.g. 120 Hz to cut ~470 msg/s of per-message framing on the
 * device. Raw IMU bundles and on-device recordings are never affected.
 *
 * Still honoured for fielded app builds: a receiver expands it into the
 * equivalent policy (cap every Quaternion channel), so old and new clients
 * share one enforcement path.
 *
 * @generated from message visio_schema.v1.control.SetImuLiveRate
 * @deprecated
 */
export type SetImuLiveRate = Message<"visio_schema.v1.control.SetImuLiveRate"> & {
  /**
   * 0 = full rate
   *
   * @generated from field: uint32 rate_hz = 1;
   */
  rateHz: number;
};

/**
 * Describes the message visio_schema.v1.control.SetImuLiveRate.
 * Use `create(SetImuLiveRateSchema)` to create a new message.
 * @deprecated
 */
export const SetImuLiveRateSchema: GenMessage<SetImuLiveRate> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 8);

/**
 * Set the auto-start-at-boot recording policy (persisted; next-boot effect,
 * does not toggle the current session). Replaces POST /recording/auto_start.
 *
 * @generated from message visio_schema.v1.control.SetAutoStart
 */
export type SetAutoStart = Message<"visio_schema.v1.control.SetAutoStart"> & {
  /**
   * @generated from field: bool auto_start = 1;
   */
  autoStart: boolean;
};

/**
 * Describes the message visio_schema.v1.control.SetAutoStart.
 * Use `create(SetAutoStartSchema)` to create a new message.
 */
export const SetAutoStartSchema: GenMessage<SetAutoStart> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 9);

/**
 * Enable/disable automatic S3 upload of finished recordings. Persisted to a
 * device-side sidecar and applied without a
 * reboot: while disabled the uploader starts no new uploads and stays idle (an
 * in-flight upload finishes; the stored creds + queue are NOT cleared), so
 * re-enabling resumes. Default is enabled. The current value is reported in
 * DeviceState.auto_upload_enabled.
 * Turn periodic status reporting on or off at runtime. Persisted device-side, so
 * it survives a reboot. Independent of SetAutoUpload, which gates the RECORDING
 * upload leg — the two share credentials but nothing else.
 *
 * @generated from message visio_schema.v1.control.SetStatusReport
 */
export type SetStatusReport = Message<"visio_schema.v1.control.SetStatusReport"> & {
  /**
   * @generated from field: bool enabled = 1;
   */
  enabled: boolean;
};

/**
 * Describes the message visio_schema.v1.control.SetStatusReport.
 * Use `create(SetStatusReportSchema)` to create a new message.
 */
export const SetStatusReportSchema: GenMessage<SetStatusReport> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 10);

/**
 * @generated from message visio_schema.v1.control.SetAutoUpload
 */
export type SetAutoUpload = Message<"visio_schema.v1.control.SetAutoUpload"> & {
  /**
   * @generated from field: bool enabled = 1;
   */
  enabled: boolean;
};

/**
 * Describes the message visio_schema.v1.control.SetAutoUpload.
 * Use `create(SetAutoUploadSchema)` to create a new message.
 */
export const SetAutoUploadSchema: GenMessage<SetAutoUpload> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 11);

/**
 * Enable/disable microphone capture reaching recordings. Persisted to a
 * device-side sidecar and applied WITHOUT a reboot: the capture thread keeps
 * draining the audio codec (so re-enabling is sample-accurate and never replays
 * a stale backlog) and simply stops publishing. Because both the device's own
 * MCAP and the phone-side MCAP
 * record what reaches the bus, this one switch covers both — a recording made
 * while disabled has no audio channel at all, not an empty one.
 *
 * Only boards with a mic accept this; every other device rejects it, which is
 * EXPECTED. Default is enabled, so existing units are unaffected by an upgrade.
 * The current value is reported in DeviceState.audio_recording, which also
 * tells the app whether to offer the control at all.
 *
 * ALWAYS address this to a specific device: an empty Command.target_device is
 * a broadcast, which on a suit would flip every mic-equipped board at once.
 *
 * Note on downgrade: the sidecar survives a rollback to firmware that predates
 * this command, and such firmware records audio unconditionally — so a device
 * downgraded while disabled resumes recording audio until re-upgraded.
 *
 * @generated from message visio_schema.v1.control.SetAudioRecording
 */
export type SetAudioRecording = Message<"visio_schema.v1.control.SetAudioRecording"> & {
  /**
   * @generated from field: bool enabled = 1;
   */
  enabled: boolean;
};

/**
 * Describes the message visio_schema.v1.control.SetAudioRecording.
 * Use `create(SetAudioRecordingSchema)` to create a new message.
 */
export const SetAudioRecordingSchema: GenMessage<SetAudioRecording> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 12);

/**
 * Enable/disable the on-device NPU hand detector (the boxes the app overlays on
 * the preview, and the spoken "hand out of view" notices that ride on it).
 *
 * Persisted to a device-side sidecar and applied LIVE, within about a second —
 * no reboot. A capable device always carries the detector's camera tap; the
 * switch starts and stops the NPU context behind it, so switched OFF the
 * detector holds no model, no memory and no thread, and the tap costs the
 * capture pipeline a single atomic read per frame.
 *
 * Default is DISABLED: running it costs NPU memory bandwidth and a block of
 * contiguous memory the camera pipeline would otherwise have, so it is opt-in
 * per unit rather than a property of the image.
 *
 * Only boards whose config stages a detector model accept this; every other
 * device rejects it, which is EXPECTED. The current value is reported in
 * DeviceState.hand_detection, which also tells the app whether to offer the
 * control at all.
 *
 * ALWAYS address this to a specific device: an empty Command.target_device is
 * a broadcast, which on a suit would flip every detector-capable board at once.
 *
 * @generated from message visio_schema.v1.control.SetHandDetection
 */
export type SetHandDetection = Message<"visio_schema.v1.control.SetHandDetection"> & {
  /**
   * @generated from field: bool enabled = 1;
   */
  enabled: boolean;
};

/**
 * Describes the message visio_schema.v1.control.SetHandDetection.
 * Use `create(SetHandDetectionSchema)` to create a new message.
 */
export const SetHandDetectionSchema: GenMessage<SetHandDetection> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 13);

/**
 * Enable/disable geo-tagging of recordings on this device.
 *
 * The boards have no GNSS receiver, so a host is the only source of a fix
 * (SetTime, SetRecordingMeta, StartRecording). This switch is what a host
 * cannot express by simply withholding one: a 0 fix KEEPS the stored
 * coordinates, so a silent host leaves the device stamping the last position
 * it was ever told. Switched OFF the device stops stamping coordinates into
 * new sessions AND stops storing incoming ones, so neither a second host nor
 * an auto-started boot recording can re-tag it.
 *
 * Persisted to a device-side sidecar. Takes effect on the next recording; it
 * does NOT retag the in-progress session, and it never rewrites a session
 * already on the card. Default is ENABLED — every unit shipped so far tags,
 * and firmware that predates this switch must not appear to have it off.
 *
 * A device switched off keeps its stored fix; switching back on resumes
 * stamping it until a host pushes a fresher one. The current value is
 * reported in DeviceState.gps_tagging.
 *
 * @generated from message visio_schema.v1.control.SetGpsTagging
 */
export type SetGpsTagging = Message<"visio_schema.v1.control.SetGpsTagging"> & {
  /**
   * @generated from field: bool enabled = 1;
   */
  enabled: boolean;
};

/**
 * Describes the message visio_schema.v1.control.SetGpsTagging.
 * Use `create(SetGpsTaggingSchema)` to create a new message.
 */
export const SetGpsTaggingSchema: GenMessage<SetGpsTagging> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 14);

/**
 * How much the device's diagnostic log records to the SD-card tier
 * (docs/protocol/diag_log.md). Persisted to a device-side sidecar and applied
 * without a reboot. 0 = the default; higher levels add more of the 5 s
 * telemetry window and the kernel ring. The flash tier is NOT affected — it is
 * curated and small by design, and a reviewer asking for "more detail" wants
 * the card tier. The RMA loop uses this: a reviewer moves a ticket to
 * need_info, the app raises the level, the customer uses the rig for a week
 * and re-submits.
 *
 * @generated from message visio_schema.v1.control.SetDiagVerbosity
 */
export type SetDiagVerbosity = Message<"visio_schema.v1.control.SetDiagVerbosity"> & {
  /**
   * @generated from field: uint32 level = 1;
   */
  level: number;
};

/**
 * Describes the message visio_schema.v1.control.SetDiagVerbosity.
 * Use `create(SetDiagVerbositySchema)` to create a new message.
 */
export const SetDiagVerbositySchema: GenMessage<SetDiagVerbosity> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 15);

/**
 * Persist the camera H.265 target bitrate to a device-side sidecar, applied to
 * EVERY camera. Like SetAutoStart, this is persisted but takes effect on the
 * NEXT BOOT only — the running encoder is not retuned live — so the app should
 * tell the user to reboot the device for the new bitrate to apply. The current
 * persisted value is echoed back in DeviceState.video_bitrate_kbps.
 *
 * @generated from message visio_schema.v1.control.SetBitrate
 */
export type SetBitrate = Message<"visio_schema.v1.control.SetBitrate"> & {
  /**
   * target CBR bitrate (kbit/s) for all cameras
   *
   * @generated from field: uint32 bitrate_kbps = 1;
   */
  bitrateKbps: number;
};

/**
 * Describes the message visio_schema.v1.control.SetBitrate.
 * Use `create(SetBitrateSchema)` to create a new message.
 */
export const SetBitrateSchema: GenMessage<SetBitrate> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 16);

/**
 * Persist the camera capture resolution to a device-side sidecar, applied to
 * EVERY camera. Like SetBitrate, this is persisted but takes effect on the NEXT
 * BOOT only — the running ISP/encoder pipeline is not reconfigured live — so
 * the app should tell the user to reboot the device for the new resolution to
 * apply. The sensor driver must support the requested geometry; an unsupported
 * size is snapped to the sensor's default mode at boot. The current persisted
 * value is echoed back in DeviceState.video_width/height.
 *
 * @generated from message visio_schema.v1.control.SetResolution
 */
export type SetResolution = Message<"visio_schema.v1.control.SetResolution"> & {
  /**
   * capture width in pixels, for all cameras
   *
   * @generated from field: uint32 width = 1;
   */
  width: number;

  /**
   * capture height in pixels, for all cameras
   *
   * @generated from field: uint32 height = 2;
   */
  height: number;
};

/**
 * Describes the message visio_schema.v1.control.SetResolution.
 * Use `create(SetResolutionSchema)` to create a new message.
 */
export const SetResolutionSchema: GenMessage<SetResolution> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 17);

/**
 * Persist default recording metadata to a device-side sidecar. Applied to
 * EVERY new session until changed — including auto-started boot recordings,
 * which carry no app input.
 * Persisted; takes effect on the next recording or reboot (does NOT retag the
 * in-progress session). An explicit StartRecording.{task,location} overrides
 * these defaults for that one session. Answered by a CommandResult (ok +
 * DeviceState echoing the persisted labels — the fleet ids below are the one
 * exception, see there).
 *
 * @generated from message visio_schema.v1.control.SetRecordingMeta
 */
export type SetRecordingMeta = Message<"visio_schema.v1.control.SetRecordingMeta"> & {
  /**
   * free-text capture task / label
   *
   * @generated from field: string task = 1;
   */
  task: string;

  /**
   * human-readable place (user-entered)
   *
   * @generated from field: string location = 2;
   */
  location: string;

  /**
   * free-text note / annotation
   *
   * @generated from field: string message = 3;
   */
  message: string;

  /**
   * who operated the capture (user-entered)
   *
   * @generated from field: string capturer = 8;
   */
  capturer: string;

  /**
   * GPS + host time/zone, mirroring StartRecording — persisted so auto-started
   * boot recordings (no app input) still carry them. All optional; 0 = unknown.
   * Unlike the text fields (where empty CLEARS the stored value), a 0 fix
   * KEEPS the stored coordinates — hosts without a fix send 0, and wiping the
   * last known position (also pushed via SetTime) would lose data. Coordinates
   * are machine-owned, not user-cleared.
   *
   * GPS; 0 when unknown (keeps stored value)
   *
   * @generated from field: double latitude = 4;
   */
  latitude: number;

  /**
   * GPS; 0 when unknown (keeps stored value)
   *
   * @generated from field: double longitude = 5;
   */
  longitude: number;

  /**
   * host reference timestamp (us since epoch)
   *
   * @generated from field: int64 client_unix_us = 6;
   */
  clientUnixUs: bigint;

  /**
   * minutes east of UTC (+480 = UTC+8)
   *
   * @generated from field: sint32 client_utc_offset_min = 7;
   */
  clientUtcOffsetMin: number;

  /**
   * Fleet identifiers: which operator account and which environment/site a rig
   * is capturing under. They ride into every session's capture metadata beside
   * the labels above, but no user-facing client sets them — they are for
   * provisioning tooling and fleet scripts, over this same command. Unlike the
   * labels, they are NOT echoed on DeviceState: a setter confirms them by
   * reading a recording, not from the ack.
   *
   * `optional` is load-bearing, not decoration. A client sends this message as
   * a whole and the device REPLACES the stored text with what arrives, so a
   * plain field would be cleared by every app or web-page "Set" — wiping an id
   * that UI never showed and whoever is holding the phone cannot restore.
   * With presence: ABSENT keeps the stored value, PRESENT replaces it, and
   * present-but-empty clears it. That makes clearing an id an explicit act by
   * a client that knows the field exists, which is the same protection the
   * 0-means-keep rule gives the coordinates above.
   *
   * @generated from field: optional string operator_id = 9;
   */
  operatorId?: string | undefined;

  /**
   * @generated from field: optional string environment_id = 10;
   */
  environmentId?: string | undefined;
};

/**
 * Describes the message visio_schema.v1.control.SetRecordingMeta.
 * Use `create(SetRecordingMetaSchema)` to create a new message.
 */
export const SetRecordingMetaSchema: GenMessage<SetRecordingMeta> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 18);

/**
 * Set the device's wall clock + UTC offset. The boards have no RTC and boot
 * to 1970-01-01, so a host pushes the real time (typically right after it
 * connects, before recording) so session timestamps are correct. Answered by a
 * CommandResult (ok + DeviceState).
 *
 * The host's GPS fix rides the same push: the boards have no GNSS receiver, so
 * the phone is the only source of coordinates, and clock + position are both
 * "where/when is this rig right now" host references. A non-zero fix is
 * persisted into the recording-metadata sidecar (the same latitude/longitude
 * SetRecordingMeta carries) and stamped into every new session; 0 = no fix,
 * and the device keeps its last stored coordinates.
 *
 * @generated from message visio_schema.v1.control.SetTime
 */
export type SetTime = Message<"visio_schema.v1.control.SetTime"> & {
  /**
   * wall-clock time, us since Unix epoch (UTC)
   *
   * @generated from field: int64 unix_us = 1;
   */
  unixUs: bigint;

  /**
   * minutes east of UTC (+480 = UTC+8)
   *
   * @generated from field: sint32 utc_offset_min = 2;
   */
  utcOffsetMin: number;

  /**
   * GPS; 0 when unknown (keeps stored value)
   *
   * @generated from field: double latitude = 3;
   */
  latitude: number;

  /**
   * GPS; 0 when unknown (keeps stored value)
   *
   * @generated from field: double longitude = 4;
   */
  longitude: number;
};

/**
 * Describes the message visio_schema.v1.control.SetTime.
 * Use `create(SetTimeSchema)` to create a new message.
 */
export const SetTimeSchema: GenMessage<SetTime> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 19);

/**
 * Provision Wi-Fi STA credentials and (re)associate. Replaces POST /wifi.
 *
 * On success the network is REMEMBERED and moves to the FRONT of
 * DeviceState.wifi_networks — the list the device walks, in order, whenever it
 * is offline. Re-provisioning an SSID that is already remembered updates its
 * passphrase in place and moves it to the front; it never creates a duplicate.
 * There is deliberately no reorder command: joining IS the reorder.
 *
 * @generated from message visio_schema.v1.control.ConnectWifi
 */
export type ConnectWifi = Message<"visio_schema.v1.control.ConnectWifi"> & {
  /**
   * @generated from field: string ssid = 1;
   */
  ssid: string;

  /**
   * @generated from field: string passphrase = 2;
   */
  passphrase: string;
};

/**
 * Describes the message visio_schema.v1.control.ConnectWifi.
 * Use `create(ConnectWifiSchema)` to create a new message.
 */
export const ConnectWifiSchema: GenMessage<ConnectWifi> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 20);

/**
 * Trigger a Wi-Fi scan; the CommandResult carries WifiScanResults. Replaces
 * GET /wifi/scan.
 *
 * @generated from message visio_schema.v1.control.ScanWifi
 */
export type ScanWifi = Message<"visio_schema.v1.control.ScanWifi"> & {
};

/**
 * Describes the message visio_schema.v1.control.ScanWifi.
 * Use `create(ScanWifiSchema)` to create a new message.
 */
export const ScanWifiSchema: GenMessage<ScanWifi> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 21);

/**
 * Forget ONE remembered Wi-Fi network, by SSID. ResetToAp forgets them all;
 * this is the per-network door.
 *
 * A LIST EDIT, NOT A RADIO ACTION. The entry is removed from the remembered set
 * so the device never auto-joins it again, and nothing else changes. In
 * particular, forgetting the SSID the device is currently associated with does
 * NOT drop that association: the link this command arrived over survives, the
 * CommandResult actually gets back, and the device simply will not rejoin after
 * the next disconnect. To leave a network *now*, use ResetToAp.
 *
 * That restraint is the whole point. ConnectWifi and ResetToAp both cost the
 * caller its link by design, and every client carries follow/rollback machinery
 * for it; ForgetWifi needs none, on any client.
 *
 * IDEMPOTENT: an ssid that is not remembered answers ok=true, not an error.
 * Two clients rendering the same polled list must not race into a spurious
 * failure, and "already gone" is the desired end state.
 *
 * @generated from message visio_schema.v1.control.ForgetWifi
 */
export type ForgetWifi = Message<"visio_schema.v1.control.ForgetWifi"> & {
  /**
   * @generated from field: string ssid = 1;
   */
  ssid: string;
};

/**
 * Describes the message visio_schema.v1.control.ForgetWifi.
 * Use `create(ForgetWifiSchema)` to create a new message.
 */
export const ForgetWifiSchema: GenMessage<ForgetWifi> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 22);

/**
 * Forget ALL remembered Wi-Fi STA credentials and return to the setup soft-AP.
 *
 * A one-shot ACTION, not a mode: the device erases EVERY stored network (so it
 * will not rejoin any of them on the next boot), drops any STA association, and
 * brings the AP back up. Sending it while already on the AP is a no-op that
 * still clears the stored credentials. Use ForgetWifi to drop a single entry.
 *
 * Like ConnectWifi, the CommandResult usually never reaches a caller that is
 * talking over the STA link, because the device tears that link down to switch
 * radios (it cannot hold an AP and an STA association at once). Callers should
 * treat a post-send transport drop as success and rediscover the device on its AP.
 *
 * @generated from message visio_schema.v1.control.ResetToAp
 */
export type ResetToAp = Message<"visio_schema.v1.control.ResetToAp"> & {
};

/**
 * Describes the message visio_schema.v1.control.ResetToAp.
 * Use `create(ResetToApSchema)` to create a new message.
 */
export const ResetToApSchema: GenMessage<ResetToAp> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 23);

/**
 * Persist S3 storage credentials. Replaces POST /storage.
 *
 * @generated from message visio_schema.v1.control.SetStorage
 */
export type SetStorage = Message<"visio_schema.v1.control.SetStorage"> & {
  /**
   * @generated from field: string endpoint_url = 1;
   */
  endpointUrl: string;

  /**
   * @generated from field: string region = 2;
   */
  region: string;

  /**
   * @generated from field: string bucket = 3;
   */
  bucket: string;

  /**
   * @generated from field: string access_key_id = 4;
   */
  accessKeyId: string;

  /**
   * @generated from field: string secret_access_key = 5;
   */
  secretAccessKey: string;

  /**
   * @generated from field: string prefix = 6;
   */
  prefix: string;

  /**
   * Key prefix for periodic STATUS REPORTS (device health + a low-res camera
   * JPEG), written under the same credentials as `prefix` above but to a
   * separate subtree with its own lifecycle and its own read grant. Empty means
   * the device's default ("status/"). The two legs are independent: a device may
   * upload recordings only, report status only, or both.
   *
   * @generated from field: string status_prefix = 7;
   */
  statusPrefix: string;

  /**
   * A `visio-seal-v1` envelope carrying this destination's SECRET access key
   * (the format lives in visio_schema.crypto.envelope). Written by a v2 settings
   * QR, which never prints the secret: the companion app relays these bytes
   * verbatim and cannot read them, and only a device holding the fleet
   * private key can open them.
   *
   * Takes precedence over `secret_access_key` when both are set. An envelope
   * that carries no `sk` leaves the stored secret alone, exactly as an empty
   * `secret_access_key` does — so a "just repoint my bucket" QR needs no
   * credential at all.
   *
   * @generated from field: bytes sealed = 8;
   */
  sealed: Uint8Array;
};

/**
 * Describes the message visio_schema.v1.control.SetStorage.
 * Use `create(SetStorageSchema)` to create a new message.
 */
export const SetStorageSchema: GenMessage<SetStorage> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 24);

/**
 * Validate S3 credentials WITHOUT persisting (a max-keys=1 list under the
 * recordings prefix, so the key needs only put + list — no bucket-info
 * grant). Replaces POST /storage/test. Same fields as SetStorage.
 *
 * @generated from message visio_schema.v1.control.TestStorage
 */
export type TestStorage = Message<"visio_schema.v1.control.TestStorage"> & {
  /**
   * @generated from field: string endpoint_url = 1;
   */
  endpointUrl: string;

  /**
   * @generated from field: string region = 2;
   */
  region: string;

  /**
   * @generated from field: string bucket = 3;
   */
  bucket: string;

  /**
   * @generated from field: string access_key_id = 4;
   */
  accessKeyId: string;

  /**
   * @generated from field: string secret_access_key = 5;
   */
  secretAccessKey: string;

  /**
   * @generated from field: string prefix = 6;
   */
  prefix: string;

  /**
   * Key prefix for periodic STATUS REPORTS (device health + a low-res camera
   * JPEG), written under the same credentials as `prefix` above but to a
   * separate subtree with its own lifecycle and its own read grant. Empty means
   * the device's default ("status/"). The two legs are independent: a device may
   * upload recordings only, report status only, or both.
   *
   * @generated from field: string status_prefix = 7;
   */
  statusPrefix: string;

  /**
   * The same envelope as SetStorage.sealed. A credential the device will
   * ACCEPT must also be one it can be asked to TEST, or "test before save"
   * breaks for exactly the sealed flow that needs it most.
   * tests/test_nanopb_options.py pins the two caps equal.
   *
   * @generated from field: bytes sealed = 8;
   */
  sealed: Uint8Array;
};

/**
 * Describes the message visio_schema.v1.control.TestStorage.
 * Use `create(TestStorageSchema)` to create a new message.
 */
export const TestStorageSchema: GenMessage<TestStorage> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 25);

/**
 * List recorded sessions + their upload state; the CommandResult carries
 * RecordingsList. Replaces GET /recordings.
 *
 * With neither `cursor` nor `session_name` set this is the original call: the
 * newest `limit` sessions, no per-file detail. See docs/protocol/recordings_pull.md.
 *
 * @generated from message visio_schema.v1.control.ListRecordings
 */
export type ListRecordings = Message<"visio_schema.v1.control.ListRecordings"> & {
  /**
   * Max entries to return (newest first). 0 = a device-chosen default.
   *
   * @generated from field: uint32 limit = 1;
   */
  limit: number;

  /**
   * "" = the first page; otherwise RecordingsList.next_cursor from the previous
   * page, passed back verbatim. Opaque to the host.
   *
   * @generated from field: string cursor = 2;
   */
  cursor: string;

  /**
   * Set = list only this session, WITH its files (RecordingEntry.files).
   * Unset = a page of sessions without files. A session that does not exist
   * answers ok=false, error_code "no_such_session".
   *
   * @generated from field: string session_name = 3;
   */
  sessionName: string;
};

/**
 * Describes the message visio_schema.v1.control.ListRecordings.
 * Use `create(ListRecordingsSchema)` to create a new message.
 */
export const ListRecordingsSchema: GenMessage<ListRecordings> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 26);

/**
 * Open one recording file for a pull. The CommandResult carries
 * RecordingFileOpen; the bytes then arrive on a bare TCP socket: the host
 * connects to RecordingFileOpen.port on the device address it sent this open
 * to, reads until the device closes, and never writes. Exactly `length` bytes
 * means the file (from `offset`) arrived whole; fewer means open again at the
 * bytes received. The open is single-use, belongs to the requesting host's
 * address, and lapses after 10 s unused. Allowed over any direct IP link
 * (USB-NCM, the setup hotspot, a joined Wi-Fi network). See
 * docs/protocol/recordings_pull.md.
 *
 * Refusals (CommandResult.error_code): "busy_recording", "writing", "busy",
 * "forbidden_link" (a hub's leaf link, an unknown endpoint), "no_data_link" (USB serial: no
 * IP path for the socket), "no_sdcard", "no_such_session", "no_such_file",
 * "changed", "out_of_range", "invalid_request", "unsupported".
 *
 * @generated from message visio_schema.v1.control.OpenRecordingFile
 */
export type OpenRecordingFile = Message<"visio_schema.v1.control.OpenRecordingFile"> & {
  /**
   * @generated from field: string session_name = 1;
   */
  sessionName: string;

  /**
   * @generated from field: string file_name = 2;
   */
  fileName: string;

  /**
   * First byte to send; resume point. == file size is valid (zero bytes follow).
   *
   * @generated from field: uint64 offset = 3;
   */
  offset: bigint;

  /**
   * Identity from the listing (or a partial download's sidecar). Both 0 =
   * unchecked; otherwise both must match or the open answers "changed".
   *
   * @generated from field: uint64 expect_size = 4;
   */
  expectSize: bigint;

  /**
   * @generated from field: uint64 expect_mtime_ns = 5;
   */
  expectMtimeNs: bigint;
};

/**
 * Describes the message visio_schema.v1.control.OpenRecordingFile.
 * Use `create(OpenRecordingFileSchema)` to create a new message.
 */
export const OpenRecordingFileSchema: GenMessage<OpenRecordingFile> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 27);

/**
 * Delete one whole recorded session from local storage. Command.target_device
 * MUST name this unit (a broadcast delete answers "invalid_request"). Allowed
 * on every direct link, USB serial included.
 *
 * Refusals: "busy_recording", "active_session", "protected" (encrypted parts,
 * diagnostic files, or an upload of the session in flight), "busy",
 * "forbidden_link" (a hub's leaf link, an unknown endpoint), "no_sdcard", "no_such_session",
 * "delete_failed", "invalid_request", "unsupported".
 *
 * @generated from message visio_schema.v1.control.DeleteRecording
 */
export type DeleteRecording = Message<"visio_schema.v1.control.DeleteRecording"> & {
  /**
   * @generated from field: string session_name = 1;
   */
  sessionName: string;
};

/**
 * Describes the message visio_schema.v1.control.DeleteRecording.
 * Use `create(DeleteRecordingSchema)` to create a new message.
 */
export const DeleteRecordingSchema: GenMessage<DeleteRecording> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 28);

/**
 * Pull the device's operational-state snapshot; the CommandResult carries
 * DeviceState. Replaces GET /info.
 *
 * @generated from message visio_schema.v1.control.GetState
 */
export type GetState = Message<"visio_schema.v1.control.GetState"> & {
};

/**
 * Describes the message visio_schema.v1.control.GetState.
 * Use `create(GetStateSchema)` to create a new message.
 */
export const GetStateSchema: GenMessage<GetState> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 29);

/**
 * Reformat the recording SD card, then re-mount it. Manufacturing / quality-
 * check path for a sealed unit that has no shell — the device unmounts the card,
 * runs mkfs, re-mounts, and re-creates the recording dir. Any active recording
 * is stopped (and finalized) first, and ALL data on the card is destroyed.
 * Answered by a CommandResult (ok + DeviceState; disk_free_pct reflects the
 * freshly-formatted card). The device needs the matching mkfs on its rootfs
 * (ext4/exfat/vfat); an unsupported type is refused (ok=false).
 *
 * A card with NO partition table is partitioned first (one primary partition
 * spanning the card, 4 MiB-aligned) — otherwise there would be no partition
 * device for mkfs to act on, and a sealed unit could never accept a wiped card.
 * A card that already carries any partition keeps its table; one that is
 * physically absent is refused ("no_sdcard").
 *
 * @generated from message visio_schema.v1.control.FormatStorage
 */
export type FormatStorage = Message<"visio_schema.v1.control.FormatStorage"> & {
  /**
   * Target filesystem. Empty = preserve the card's current type (detected from
   * the on-disk superblock; a blank card or an unsupported filesystem falls
   * back to FAT32/vfat). Explicit "ext4" / "exfat" / "vfat" forces a type —
   * ask for ext4 on a card that will be recorded to hard, since FAT32 does not
   * journal through a mid-write power-off.
   *
   * @generated from field: string fs_type = 1;
   */
  fsType: string;
};

/**
 * Describes the message visio_schema.v1.control.FormatStorage.
 * Use `create(FormatStorageSchema)` to create a new message.
 */
export const FormatStorageSchema: GenMessage<FormatStorage> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 30);

/**
 * Select the language of the device's spoken voice notices (boards with a
 * speaker; speakerless boards accept and ignore it). Applied immediately AND
 * persisted to a device-side sidecar. The app typically sends its phone locale
 * right after connecting. An unknown/unshipped language falls back to the
 * device default (English). Answered by a CommandResult (ok + DeviceState).
 *
 * @generated from message visio_schema.v1.control.SetNoticeLang
 */
export type SetNoticeLang = Message<"visio_schema.v1.control.SetNoticeLang"> & {
  /**
   * Lowercase ISO 639-1 code of the primary language ("en", "zh"). Region
   * subtags are ignored by the device ("zh-CN" -> "zh").
   *
   * @generated from field: string lang = 1;
   */
  lang: string;
};

/**
 * Describes the message visio_schema.v1.control.SetNoticeLang.
 * Use `create(SetNoticeLangSchema)` to create a new message.
 */
export const SetNoticeLangSchema: GenMessage<SetNoticeLang> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 31);

/**
 * Set the loudness of the device's spoken voice notices. Persisted to a
 * device-side sidecar (notice_volume.json under the device state dir) and
 * applied WITHOUT a reboot — the next notice plays at the new volume. Every
 * notice is affected: announcements, error notices, and the recording
 * heartbeat alike.
 *
 * Only boards with a speaker + voice notices accept this; every other device
 * rejects it ("unsupported"), which is EXPECTED. A volume above 100 is
 * rejected ("out_of_range"). The current value is reported in
 * DeviceState.notice_volume, whose presence also tells the app whether to
 * offer the control at all.
 *
 * Note on downgrade: the sidecar survives a rollback to firmware that
 * predates this command, and such firmware plays notices at the board
 * config's full loudness — so a device that was turned down (or muted)
 * comes back loud until re-upgraded.
 *
 * @generated from message visio_schema.v1.control.SetNoticeVolume
 */
export type SetNoticeVolume = Message<"visio_schema.v1.control.SetNoticeVolume"> & {
  /**
   * 0-100. 100 plays at the board config's full loudness; values in between
   * scale the codec's playback gain; 0 MUTES notices entirely (the hardware
   * floor is quiet but not silent, so the device skips playback instead).
   *
   * @generated from field: uint32 volume = 1;
   */
  volume: number;
};

/**
 * Describes the message visio_schema.v1.control.SetNoticeVolume.
 * Use `create(SetNoticeVolumeSchema)` to create a new message.
 */
export const SetNoticeVolumeSchema: GenMessage<SetNoticeVolume> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 32);

/**
 * Persist one piece of sensor calibration (effective on next boot). The target
 * device is the enclosing Command.target_device (end-to-end, hub-forwarded), so
 * a host can address a specific relayed leaf by name. The artifact is routed by
 * (sensor_kind, sensor_index, which_artifact) to the matching topic + store:
 *   intrinsics  -> /<dev>/camera/<i>/intrinsics  (foxglove.CameraCalibration)
 *   extrinsics  -> /<dev>/camera/<i>/extrinsics or /<dev>/imu/<i>/extrinsics
 *                  (foxglove.FrameTransform, child posed in the cam0 frame)
 *   tcp_extrinsics -> /<dev>/camera/<i>/tcp_extrinsics
 *                  (foxglove.FrameTransform, index 0 only)
 *   imu_info    -> /<dev>/imu/<i>/info           (ImuCalibration)
 *   encoder_info-> /<dev>/encoder/<i>/info       (EncoderCalibration)
 *   camera_tuning -> persisted only; NO topic    (CameraTuning)
 * Answered by a CommandResult (ok + DeviceState), correlated by command_id.
 *
 * camera_tuning is the one artifact that is not re-published. It is a per-unit
 * ISP measurement the device consumes to trim its own AWB — nothing downstream
 * consumes it, so it is stored and applied but never broadcast. That also means
 * the CommandResult is the ONLY acknowledgement: unlike the other artifacts,
 * there is no 1 Hz re-broadcast to confirm against.
 * `unit_side` is here, among per-sensor artifacts, because it IS calibration:
 * which limb a mirrored gripper is cannot be read off the board, one firmware
 * image serves both, and the extrinsics carried by this same message already
 * encode the side — a left unit's translation and rotation are not a right
 * unit's. Delivering the label through the same channel as the geometry it
 * labels is what stops the two disagreeing. The device validates it and refuses
 * anything but "left" or "right"; absent leaves the unit unassigned, which it
 * reports by rooting its topics at its own GILABS label rather than guessing.
 *
 * @generated from message visio_schema.v1.control.SetCalibration
 */
export type SetCalibration = Message<"visio_schema.v1.control.SetCalibration"> & {
  /**
   * which sensor family the artifact targets
   *
   * @generated from field: visio_schema.v1.control.SetCalibration.SensorKind sensor_kind = 1;
   */
  sensorKind: SetCalibration_SensorKind;

  /**
   * 0-based instance within that family
   *
   * @generated from field: uint32 sensor_index = 2;
   */
  sensorIndex: number;

  /**
   * @generated from oneof visio_schema.v1.control.SetCalibration.artifact
   */
  artifact: {
    /**
     * CAMERA
     *
     * @generated from field: foxglove.CameraCalibration intrinsics = 11;
     */
    value: CameraCalibration;
    case: "intrinsics";
  } | {
    /**
     * CAMERA (i>0) or IMU, rel. cam0
     *
     * @generated from field: foxglove.FrameTransform extrinsics = 12;
     */
    value: FrameTransform;
    case: "extrinsics";
  } | {
    /**
     * IMU
     *
     * @generated from field: visio_schema.v1.calibration.ImuCalibration imu_info = 13;
     */
    value: ImuCalibration;
    case: "imuInfo";
  } | {
    /**
     * ENCODER
     *
     * @generated from field: visio_schema.v1.calibration.EncoderCalibration encoder_info = 14;
     */
    value: EncoderCalibration;
    case: "encoderInfo";
  } | {
    /**
     * CAMERA, set-only
     *
     * @generated from field: visio_schema.v1.calibration.CameraTuning camera_tuning = 15;
     */
    value: CameraTuning;
    case: "cameraTuning";
  } | {
    /**
     * UNIT: "left" or "right"
     *
     * @generated from field: string unit_side = 16;
     */
    value: string;
    case: "unitSide";
  } | {
    /**
     * CAMERA, and `sensor_index` MUST be 0 — the anchor. That is the inverse
     * of the `extrinsics` rule, which forbids index 0 (cam0 is the frame the
     * others are posed in, so it has no extrinsic of its own). The pose of the
     * gripper's tool-centre-point frame expressed in cam0
     * (`parent_frame_id="cam0"`, `child_frame_id="tcp"`,
     * `p_cam0 = R * p_tcp + t`): the same direction as `camera/<i>/extrinsics`.
     * Its own artifact rather than an `extrinsics` on a spare index because it
     * is not a sensor's pose but the tool's; a limb has exactly one, so it hangs
     * off the anchor camera and is re-published on
     * `/<dev>/camera/0/tcp_extrinsics`.
     *
     * 10 reserved for a deferred magnetometer artifact.
     *
     * @generated from field: foxglove.FrameTransform tcp_extrinsics = 17;
     */
    value: FrameTransform;
    case: "tcpExtrinsics";
  } | { case: undefined; value?: undefined };
};

/**
 * Describes the message visio_schema.v1.control.SetCalibration.
 * Use `create(SetCalibrationSchema)` to create a new message.
 */
export const SetCalibrationSchema: GenMessage<SetCalibration> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 33);

/**
 * @generated from enum visio_schema.v1.control.SetCalibration.SensorKind
 */
export enum SetCalibration_SensorKind {
  /**
   * @generated from enum value: SENSOR_KIND_UNSPECIFIED = 0;
   */
  SENSOR_KIND_UNSPECIFIED = 0,

  /**
   * @generated from enum value: CAMERA = 1;
   */
  CAMERA = 1,

  /**
   * @generated from enum value: IMU = 2;
   */
  IMU = 2,

  /**
   * @generated from enum value: ENCODER = 3;
   */
  ENCODER = 3,

  /**
   * The BOARD itself, not a sensor on it: `sensor_index` is unused. Carries
   * artifacts that describe the unit rather than one of its instruments —
   * today just `unit_side`. Additive: firmware that predates this kind falls
   * through its sensor_kind switch and REFUSES the command, which is the
   * right answer, rather than silently applying it to a sensor.
   *
   * @generated from enum value: UNIT = 4;
   */
  UNIT = 4,
}

/**
 * Describes the enum visio_schema.v1.control.SetCalibration.SensorKind.
 */
export const SetCalibration_SensorKindSchema: GenEnum<SetCalibration_SensorKind> = /*@__PURE__*/
  enumDesc(file_visio_schema_v1_control_command, 33, 0);

/**
 * Erase this unit's stored per-unit camera tuning (the camera_tuning record) and
 * restart so it comes back UNCORRECTED.
 *
 * A one-shot ACTION, and the counterpart to SetCalibration{camera_tuning}. It
 * exists because a new tuning cannot be measured on top of a live correction: the
 * measurement reads the residual with the correction already applied, so the
 * device REFUSES a camera_tuning write while one is active (CommandResult
 * error_code "correction_active"). Clearing removes the on-disk record; the
 * device then restarts (like a successful set) so the next boot loads no record
 * and the ISP comes up at identity — the clean baseline a re-measure needs.
 *
 * Deliberately its own command, not an empty SetCalibration: the camera_tuning
 * artifact requires >=1 point and its write path is exactly what the gate blocks,
 * so "clear" must be a separate, ungated verb. No fields — tuning is one record
 * per unit.
 *
 * @generated from message visio_schema.v1.control.ClearCameraTuning
 */
export type ClearCameraTuning = Message<"visio_schema.v1.control.ClearCameraTuning"> & {
};

/**
 * Describes the message visio_schema.v1.control.ClearCameraTuning.
 * Use `create(ClearCameraTuningSchema)` to create a new message.
 */
export const ClearCameraTuningSchema: GenMessage<ClearCameraTuning> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 34);

/**
 * Set, rotate or clear the symmetric key this unit encrypts its recordings
 * under (the `VREC` container's key).
 *
 * The key never travels in the clear. It rides inside a `visio-seal-v1`
 * envelope (visio_schema.crypto.envelope) that only a device holding the fleet
 * private key can open, so the operator who scans the QR — and the companion
 * app that relays it — handle an opaque blob. That is the point: the operator
 * is the adversary this whole path exists to defend against.
 *
 * The sealed body is compact JSON. `rk` is the new key (base64, 32 raw
 * bytes; "" clears it), `rko` the key currently on the device. Setting a key
 * where one is ALREADY set requires a matching `rko`, and that rule is the
 * only integrity control in the design — the fleet public key is published,
 * so without it anyone could seal their own key and re-key a rig they are
 * holding. An `rko` mismatch answers error_code "bad_old_key"; an envelope
 * that will not open answers "bad_envelope". Keep those distinct: one means
 * "you do not own this device", the other "this QR is for a different key
 * generation", and conflating them costs a support call.
 *
 * Applies from the NEXT recording part onward. Parts already on the card
 * carry the old key's fingerprint in their `VREC` header and stay readable
 * only under that key — so a fleet owner's key archive is append-only, and
 * rotating is not a licence to discard the previous key.
 *
 * @generated from message visio_schema.v1.control.SetRecordingKey
 */
export type SetRecordingKey = Message<"visio_schema.v1.control.SetRecordingKey"> & {
  /**
   * @generated from field: bytes sealed = 1;
   */
  sealed: Uint8Array;
};

/**
 * Describes the message visio_schema.v1.control.SetRecordingKey.
 * Use `create(SetRecordingKeySchema)` to create a new message.
 */
export const SetRecordingKeySchema: GenMessage<SetRecordingKey> = /*@__PURE__*/
  messageDesc(file_visio_schema_v1_control_command, 35);

